@* 
@{
    ViewData["Title"] = "Index";
}
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Employee Management System</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: "Segoe UI", sans-serif;
        }

        body {
            line-height: 1.6;
            color: #333;
            display: flex;
            min-height: 100vh;
        }

        /* Sidebar */
        .sidebar {
            width: 220px;
            background: #007bff;
            color: white;
            padding: 20px;
            flex-shrink: 0;
        }

            .sidebar h2 {
                text-align: center;
                margin-bottom: 30px;
                font-size: 1.5rem;
            }

            .sidebar a {
                display: block;
                color: white;
                text-decoration: none;
                padding: 12px 15px;
                border-radius: 8px;
                margin-bottom: 10px;
                transition: 0.3s;
            }

                .sidebar a:hover {
                    background: #0056b3;
                }

        /* Main content */
        .main-content {
            flex: 1;
            background: #f4f4f4;
            padding: 20px;
            overflow-y: auto;
        }

        /* Hero Section */
        .hero {
            height: 60vh;
            background: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.6)), url("https://picsum.photos/1600/900?office") center/cover no-repeat;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            color: white;
            text-align: center;
            padding: 0 20px;
            border-radius: 12px;
            margin-bottom: 30px;
        }

            .hero h1 {
                font-size: 3rem;
                margin-bottom: 20px;
            }

            .hero p {
                font-size: 1.2rem;
                margin-bottom: 30px;
            }

        /* Features */
        .features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }

        .feature {
            background: white;
            padding: 20px;
            border-radius: 12px;
            text-align: center;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            transition: 0.3s;
        }

            .feature:hover {
                transform: translateY(-5px);
            }

            .feature h3 {
                margin-bottom: 15px;
                color: #007bff;
            }

        /* About Section */
        .about {
            padding: 20px;
            background: white;
            border-radius: 12px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            margin-bottom: 30px;
        }

            .about h2 {
                margin-bottom: 20px;
                font-size: 2rem;
                color: #222;
            }

            .about p {
                max-width: 700px;
                margin: 0 auto;
                font-size: 1.1rem;
                color: #555;
            }

        /* Footer */
        footer {
            background: #222;
            color: #ccc;
            text-align: center;
            padding: 20px;
            border-radius: 12px;
            margin-top: 20px;
        }
    </style>
</head>
<body>

    <!-- Sidebar -->
    <div class="sidebar">
        <h2>EMS Menu</h2>
        <a href="#">Employee</a>
        <a class="nav-link text-white" asp-controller="Employee" asp-action="Index">
            <i class="bi bi-people"></i> Employees
        </a>
        <a href="#">Department</a>
        <a href="#">Salary</a>
        <a href="#">Payroll</a>
    </div>

    <!-- Main Content -->
    <div class="main-content">

        <!-- Hero -->
        <section class="hero">
            <h1>Employee Management System</h1>
            <p>Manage employees, payroll, and attendance in one place.</p>
        </section>

        <!-- Features -->
        <section id="features" class="features">
            <div class="feature">
                <h3>👨‍💼 Employee Records</h3>
                <p>Store and manage complete employee information with ease.</p>
            </div>
            <div class="feature">
                <h3>💰 Payroll</h3>
                <p>Automate salary processing and keep financial records secure.</p>
            </div>
            <div class="feature">
                <h3>⏰ Attendance</h3>
                <p>Track daily attendance and leave management seamlessly.</p>
            </div>
        </section>

        <!-- About -->
        <section class="about">
            <h2>About EMS</h2>
            <p>
                Our Employee Management System is designed to simplify HR operations.
                With a user-friendly interface, it provides tools to manage employee data,
                payroll, and attendance in a secure and efficient manner.
            </p>
        </section>

        <!-- Footer -->
        <footer>
            <p>© 2025 Employee Management System. All rights reserved.</p>
        </footer>

    </div>

</body>
</html>

 *@
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>HR Dashboard</title>
    <!-- Bootstrap CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            min-height: 100vh;
            background: #f8f9fa;
        }

        .sidebar {
            min-width: 220px;
            max-width: 220px;
            background: #343a40;
            color: #fff;
            min-height: 100vh;
            position: fixed;
            top: 56px; /* height of navbar */
            left: 0;
            padding-top: 1rem;
        }

            .sidebar a {
                color: #fff;
                text-decoration: none;
                display: block;
                padding: 10px 20px;
            }

                .sidebar a:hover {
                    background: #495057;
                }

        .content {
            margin-left: 220px;
            padding: 2rem;
            margin-top: 56px; /* height of navbar */
        }
        

        }
    </style>
</head>
<body>
    <!-- Top Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">HR Dashboard</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                    aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <!-- Add other navbar items here if needed -->
        </div>
    </nav>

    <!-- Sidebar -->
    <div class="sidebar">
        <h5 class="px-3">Menu</h5>
        <a href="#">Employee</a>
        <a href="#">Department</a>
        <a href="#">Payroll</a>
        <a href="#">Attendance</a>
        <a href="#">Leave</a>
        <a href="#">Reports</a>
    </div>

    <!-- Main Content -->
    <div class="content">
        <h2>Welcome to the HR Dashboard</h2>
        <p>Select an item from the sidebar to view details.</p>
    </div>

    <!-- Bootstrap JS CDN -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>